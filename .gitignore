build/
.cache/
compile_commands.json
*.user

# The preset pack is fetched separately - see README. 157 MB, its own project.
assets/presets/

# Local test media, never published.
assets/test-audio/
assets/test-video/

# No media in the tree, wherever it lands. A file dropped in the project root to test with was
# swept up by `git add -A` and published; the ignore rules only covered assets/test-audio.
*.mp3
*.m4a
*.flac
*.ogg
*.opus
*.wav
*.aac
*.wma
*.mp4
*.mkv
*.webm
*.avi
*.mov

# Scan output from tools/scan-presets.sh. Data about the preset corpus, not part of the project,
# and large enough to be noise in a diff.
preset-scan*.tsv
scan.log
nohup.out

# Python bytecode from the tools, never part of the project
__pycache__/
